<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ecomm.sciproapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".base.SciProApplication"
        android:theme="@style/Theme.SciProApplication">
        <activity
            android:name=".MainAndroidViewActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SciProApplication.NoActionBar">
        </activity>

        <activity
            android:name=".views.SelectProfilActivity.SelectProfilActivity"
            android:exported="true"
            android:theme="@style/AppThemeNoActionBar"/>

        <activity
            android:name=".views.student.StudentManagerActivity"
            android:exported="true"
            android:theme="@style/AppThemeNoActionBar"/>

        <activity
            android:name=".views.teacher.TeacherManagerActivity"
            android:exported="true"
            android:theme="@style/AppThemeNoActionBar"/>

    </application>

</manifest>